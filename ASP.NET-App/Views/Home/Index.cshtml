@model HomeIndexViewModel

<h1>@Model.Title</h1>

<partial name="/Views/Partials/Components/_Showcase.cshtml" />

<section class="carousel-section">
    <div class="container">
        <div class="row">
            <div class="col">
                @foreach (var category in Model.Categories!)
                {
                    <div class="mb-4">
                        <h2>@category.Name</h2>
                        <div id="@category.Name!.ToLower()Carousel" class="carousel slide">
                            <div class="carousel-inner">
                                @for (int i = 0; i < category.ProductCategories!.Count; i += 6)
                                {
                                    <div class="carousel-item @(i == 0 ? "active" : "")">
                                        <div class="row">
                                            @foreach (var productCategory in category.ProductCategories.Skip(i).Take(6))
                                            {
                                                <div class="col-lg-2 col-md-4 col-sm-6">
                                                    <div class="card h-100 border">
                                                        <img src="/images/placeholders/@productCategory.Product!.ProductImage" alt="@productCategory.Product!.ProductName" />
                                                        <div class="card-body d-flex flex-column justify-content-between">
                                                            <h4>@productCategory.Product.ProductName</h4>
                                                            <p class="card-ingress">@productCategory.Product.Ingress</p>
                                                            <div class="mt-auto">
                                                                <a href="@Url.Action("Details", "Products", new { id = productCategory.Product.ArticleNumber })" class="btn btn-theme">Read More</a>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            }
                                        </div>
                                    </div>
                                }
                            </div>
                            <button class="carousel-control-prev" type="button" data-bs-target="#@category.Name.ToLower()Carousel" data-bs-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true" style="font-size: 1rem;"></span>
                                <span class="visually-hidden">Previous</span>
                            </button>
                            <button class="carousel-control-next" type="button" data-bs-target="#@category.Name.ToLower()Carousel" data-bs-slide="next" >
                                <span class="carousel-control-next-icon" aria-hidden="true" style="font-size: 1rem;"></span>
                                <span class="visually-hidden">Next</span>
                            </button>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</section>